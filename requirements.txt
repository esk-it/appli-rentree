# Backend FastAPI
fastapi>=0.115
uvicorn[standard]>=0.32
python-multipart>=0.0.20
pydantic>=2.9

# Données
pandas>=2.2
openpyxl>=3.1
xlrd>=2.0
lxml>=5.0

# Base de données
sqlalchemy>=2.0
aiosqlite>=0.20

# Utilitaires
unidecode>=1.3
python-dotenv>=1.0

# Google Workspace API
#
# Le mode fichier CSV reste le mode nominal et le secours : si Google refuse
# un compte, on ne veut pas être bloqué. Le code teste la présence de ces
# paquets et désactive proprement le mode API s'ils manquent.
#
# Bundlés via les hiddenimports de backend.spec — la découverte de service
# et les modules crypto sont chargés dynamiquement, PyInstaller ne les voit
# pas tout seul.
google-api-python-client>=2.100
google-auth>=2.23

# Tests
pytest>=8.3
pytest-cov>=5.0
httpx>=0.27  # pour TestClient FastAPI

# Un nom absent d'une liste d'imports ne se manifeste qu'à l'exécution de
# la ligne qui l'utilise. `test_noms_definis.py` s'appuie sur ruff (F821)
# pour le voir sans exécuter le code : sans lui, ce contrôle échouerait.
ruff>=0.6
