# Backend FastAPI
fastapi>=0.115
uvicorn[standard]>=0.32
python-multipart>=0.0.20
pydantic>=2.9

# Données
pandas>=2.2
openpyxl>=3.1
xlrd>=2.0
lxml>=5.0

# Base de données
sqlalchemy>=2.0
aiosqlite>=0.20

# Utilitaires
unidecode>=1.3
python-dotenv>=1.0

# Google Workspace API — OPTIONNEL
#
# Volontairement non installé pour l'instant : ajouter ces paquets impose de
# les bundler dans PyInstaller (hidden imports, poids, risque de casser le
# build). Le code détecte leur absence et désactive proprement le mode API —
# le mode fichier CSV, qui est le mode nominal, reste entièrement fonctionnel.
#
# À décommenter le jour où le mode API sera réellement utilisé, en vérifiant
# le build packagé dans la foulée :
# google-api-python-client>=2.100
# google-auth>=2.23

# Tests
pytest>=8.3
pytest-cov>=5.0
httpx>=0.27  # pour TestClient FastAPI
